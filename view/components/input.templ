package component


import(  
    button "github.com/josephschec/go-htmx/view/components/Button"   
)
type InputProps struct {
    Type string
    Name string
}

templ Input(props InputProps){
    <input type={props.Type} name={props.Name}/>
     @button.Button() 
}